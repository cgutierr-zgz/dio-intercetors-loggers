name: dio_interceptors_loggers
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  dio: ^4.0.6
  dio_smart_retry: ^1.3.2
  flutter:
    sdk: flutter
  flutter_appauth: ^4.2.1
  flutter_secure_storage: ^6.0.0
  pretty_dio_logger: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
